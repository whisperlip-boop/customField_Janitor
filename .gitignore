target/
amps-standalone-*/
.idea/
*.iml
.classpath
.project
.factorypath
.settings/

# 픽스처 스크립트가 만드는 인스턴스별 상태 (필드 ID는 인스턴스마다 다르다)
tools/.fixture-e-field-id

# REST 프로브 응답을 임시로 받아두는 파일 (루트에만 생긴다)
/*.json

# 인수인계 메모. 테스트 인스턴스 내부 상태를 담고 있어 공개 저장소에 올리지 않는다.
docs/HANDOFF.md

# 개인 빌드 설정(복사 위치 등). 사람마다 다르므로 저장소에 넣지 않는다.
.build.local
